# =============================================================================
# Copie para .env.local (web-frontend)
# cp .env.example .env.local
# =============================================================================

# URL do bot-backend (Railway) — sem barra no final
# Desenvolvimento local: http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
